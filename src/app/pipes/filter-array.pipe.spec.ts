import { FilterArrayPipe } from './filter-array.pipe';

describe('TakeAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
